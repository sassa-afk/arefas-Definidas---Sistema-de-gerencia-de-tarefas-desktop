version: '3.3'

services:
  postgres:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: adm
      POSTGRES_DB: tarefasdefinidas
    ports:
      - "5435"
